# Demo: Tasks Manager with React and Express

This is a simple demo application that showcases a Tasks Manager built with React for the frontend and Express for the backend. The application allows users to create, view, update, and delete tasks.

# Notes

To see the logs for the running develompment server, run the following command in a separate terminal:

```bash
tmux attach -t dev-logs
```
